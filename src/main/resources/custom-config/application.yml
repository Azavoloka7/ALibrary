# DataSource Configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3308/ALibrary
    username: root
    password: Z@v010ka
    driver-class-name: com.mysql.cj.jdbc.Driver
  logging:
    level:
      com.zavoloka: debug
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQLDialect

# Server Port Configuration
server:
  port: 8080

# Main Configuration
main:
  allow-bean-definition-overriding: true

# Logging Configuration
logging:
  level:
    org.springframework: info

# Other Configuration
# Add other properties as needed for your specific application
